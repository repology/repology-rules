# vim: tabstop=39 expandtab softtabstop=39

# Zope; needs to be before python
- { namepat: "py[0-9][0-9]-zope\\.(.*)", setname: "zope:$1", ruleset: [ freebsd ] }
- { namepat: "zope\\.(.*)",              setname: "zope:$1", ruleset: [ debuntu, slackbuilds ] }

# XXX: merge into python:
